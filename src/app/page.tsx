import { PagePlaceholder } from "@/components/layout";

export default function Home() {
  return (
    <PagePlaceholder
      title="BookExchange"
      description="Next.js, TypeScript, Tailwind and Firebase scaffold is ready."
    />
  );
}
