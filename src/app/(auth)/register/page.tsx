import { PagePlaceholder } from "@/components/layout";

export default function RegisterPage() {
  return (
    <PagePlaceholder
      title="Register"
      description="Account creation page placeholder for new users."
    />
  );
}
