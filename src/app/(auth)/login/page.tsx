import { PagePlaceholder } from "@/components/layout";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Login"
      description="Sign-in page placeholder for Firebase Authentication."
    />
  );
}
